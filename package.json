{
  "name": "cav-crm",
  "type": "module",
  "private": true,
  "packageManager": "bun@1.4.2",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test ./packages",
    "typecheck": "bun run --cwd packages/api typecheck && bun run --cwd packages/db typecheck && bun run --cwd packages/web typecheck",
    "build": "bun run --cwd packages/api build && bun run --cwd packages/web build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22",
    "bun-types": "1.4.2",
    "typescript": "^7"
  }
}
